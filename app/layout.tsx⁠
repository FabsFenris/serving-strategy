import React from 'react';

export const metadata = {
  title: 'Serving Strategy',
  description: 'Marketplace Platform',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0, fontFamily: 'sans-serif', backgroundColor: '#FAF9F6' }}>
        {children}
      </body>
    </html>
  );
}
